# Vercel
.vercel/
src/kkn-hub/.vercel/

# Session logs (huge files, not needed in repo)
last-progress_*.md

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/

# Node (kalau nanti pakai)
node_modules/
npm-debug.log

# ═══════════════════════════════════════════════════════════
# SENSITIVE — JANGAN PUSH KE PUBLIC REPO
# ═══════════════════════════════════════════════════════════
# Bukti pelunasan & dokumen personal
assets/Bukti-Pelunasan-*
assets/Surat Kesanggupan*

# API keys (kalau dipindah ke env file di masa depan)
.env
.env.local
.env.*.local
# LEAP game Supabase config (Dex isi url+anonKey lokal; anon public-safe tapi keep project URL out of repo)
src/kkn-hub/leap/leap-config.js

# ═══════════════════════════════════════════════════════════
# PUBLIC REPO — exclude internal / machine / personal (1 Jun)
# ═══════════════════════════════════════════════════════════
# Catatan kerja mentah Dex (progress raw, miskom APL, pertimbangan, patch notes)
docs/_internal/
# Setting lokal Claude Code (machine-specific)
.claude/settings.local.json
# Agent OS internals (workflow pribadi, bukan deliverable KKN)
.agent/
# Temp screenshot / preview render
*_shot_*.png
**/_preview_*.html
**/_test.js
# Dokumen milik kelompok lain — jangan publish di repo publik (working copy tetap ada)
docs/referensi/Contoh_Proposal*
